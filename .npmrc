# Expo SDK 54 pulls transitive packages (e.g. @expo/dom-webview) whose peer
# ranges npm cannot satisfy in strict mode. This is independent of the
# Next 15 / React 19 setup, which resolves cleanly on its own.
legacy-peer-deps=true
